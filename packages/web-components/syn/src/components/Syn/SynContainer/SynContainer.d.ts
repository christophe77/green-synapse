import { ReactNode } from 'react';
interface ISynContainerProps {
    children: ReactNode;
}
export default function SynContainer({ children, }: Readonly<ISynContainerProps>): import("react").JSX.Element;
export {};
