{
	"name": "green-synapse",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "yarn workspaces run build",
		"dev": "concurrently \"pnpm --filter @green-synapse/api dev\" \"pnpm --filter @green-synapse/web dev\" \"cd syn && pnpm dev -- --host\"",
		"start": "yarn workspaces run start"
	},
	"devDependencies": {
		"concurrently": "^9.1.2",
		"vite": "^6.2.0"
	}
}
