{
	"name": "green-synapse",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "pnpm -r build",
		"dev": "concurrently \"pnpm --filter @green-synapse/web dev\" \"pnpm --filter @green-synapse/web-components dev\"",
		"test": "pnpm -r test"
	},
	"devDependencies": {
		"concurrently": "^9.1.2"
	}
}
